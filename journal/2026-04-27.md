# Journal — 2026-04-27

## PR #9: Phase 1b-frontend: Kanban UI + drag-drop + Plan/Work mode + shortcuts + E2E

**Branch:** `phase-1b-frontend-impl`  
**Author:** handokobeni  
**Changes:** +1928 -36 across 23 files  
**Link:** https://github.com/handokobeni/ansambel/pull/9

### Commits

- feat(phase-1b): add Task/TaskPatch/CreateTaskArgs types and api.task IPC
  wrappers
- feat(phase-1b): add TasksStore with nested SvelteMap and listForColumn
- feat(phase-1b): add ModeStore for plan/work mode toggle
- feat(phase-1b): add TaskCard component with truncation and branch badge
- feat(phase-1b): add KanbanBoard with 4-column layout and task rendering
- feat(phase-1b): add NewTaskDialog modal with title/description form
- feat(phase-1b): wire svelte-dnd-action drag-drop in KanbanBoard
- feat(phase-1b): add Plan/Work mode toggle to TitleBar
- feat(phase-1b): add ShortcutRegistry — cross-platform keyboard shortcut system
- feat(phase-1b): wire 5 baseline shortcuts in App.svelte via ShortcutRegistry
- feat(phase-1b): integrate App.svelte with Plan/Work mode and KanbanBoard
- fix(phase-1b): merge new App into root src/App.svelte (drop duplicate stub)
- test(phase-1b): add branch coverage tests to reach 95%+ threshold
- test(phase-1b): add E2E kanban spec and extend tauri-shim with task commands
- fix(phase-1b): apply Tailwind utility styles to kanban components
- fix(phase-1b): center NewTaskDialog by replacing dialog with role=dialog div
- fix(phase-1b): propagate move_task backend response into store (workspace_id)
- fix(phase-1b): stop $effect from clobbering dnd state during re-drags
- fix(phase-1b): sync workspace_id updates in-place without re-hydrating
- fix(phase-1b): return updated task from move_task shim so workspace reload
  runs

### Summary

Implementasi lengkap frontend Phase 1b — kanban board yang terhubung ke backend
Phase 1b (task commands + persistence di Rust).

**Yang dibangun:**

- `TasksStore` — runes-based store dengan nested
  `SvelteMap<repoId, SvelteMap<taskId, Task>>`
- `TaskCard`, `KanbanBoard` (4-kolom), `NewTaskDialog` modal
- Drag-drop via `svelte-dnd-action`; drag Todo → In Progress auto-create
  workspace (backend side effect)
- Plan/Work mode toggle di TitleBar + `ModeStore`
- `ShortcutRegistry` dengan 5 shortcut baseline (⌘1/⌘2/⌘N/⌘,/⌘E)
- E2E golden path: add repo → add task → drag → assert workspace muncul di
  Sidebar

**Bug yang diperbaiki hari ini:**

- CI E2E smoke gagal: shim `move_task` mengembalikan `undefined`, menyebabkan
  `tasks.move()` crash pada `updated.repo_id` sebelum `workspaces.loadForRepo()`
  dipanggil, sehingga sidebar tidak pernah menampilkan workspace baru. Fix:
  `return task ?? undefined` di tauri-shim.

**Coverage:** 136 Rust unit tests (98.33% lines), 140 Vitest tests (96.57%
branches), 4 E2E tests

---
